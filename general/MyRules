# MyRules for Development

## Rules for this markedown Document:
-- START RULES FOR MD --
1. The sections from the # {Headername} alsways start with "-- START {header name} --" and end with "-- END {header name} --" so you know what is what
2. when searching for the header name dont be letter sensitive
-- END RULES FOR MD --

## install-requirements
-- START install-requirements --
Mache jeweils eine ir.bat und eine ir.sh die beide einfach die requirements fürs projekt installieren und überprüfen
-- END install-requirements --

## Workflow 
-- START WORKFLOW --
erstelle jetzt noch einmal github workflows: bei jedem commit einmal build und beim Release: bauen -> zip -> an release
anhängen (+ wenn sgeht denn installer zu der zip hinzufügen). 
Dazu nutze die ISSUE-TEMPLATES aus https://github.com/vynofc/SKILLS/tree/main/templates/ISSUE/
-- END WORKFLOW --
